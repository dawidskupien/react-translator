export type Dictionary = {
  compontents: {
    header: {
      title: string;
    };
    translator: {
      select: string;
      loading: string;
      error: string;
      err_button: string;
      no_langauges: string;
    };
    footer: {
      text: string;
    };
  };
};
